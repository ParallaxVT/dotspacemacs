;;; extensions.el --- rgp-rotate-text Layer packages File for Spacemacs

(defvar rotate-text-post-extensions '(rotate-text))

(defun rgp-rotate-text/init-rotate-text ()
  "Initialize rotate-text"
  (use-package rotate-text))
